# E1-T01 - Adaptador KycProvider (microservicio KYC)

## Meta
| Campo | Valor |
|---|---|
| ID | `E1-T01` |
| Modulo | `identity` |
| Tipo | `adaptador` |
| Sprint | `1` |
| HU | `HU01` |
| Dependencias | `Ninguna` |
| Estado | `Hecho` |

## Objetivo
Implementar en `app/adapters/` un cliente hacia el microservicio KYC existente, con interfaz
comun y dos implementaciones (real y mock), incluyendo timeouts, reintentos y circuit breaker.

## Contexto embebido
- El microservicio KYC solo se usa en la creacion de cuenta (HU01).
- Endpoints: `/liveness/challenge`, `/liveness/evaluate`, `/identity/verify-full`.
- Autenticacion por `X-API-Key`; la key vive en el backend y nunca llega al movil.
- No persistir frames ni imagenes.

## Context pack
- L0: este brief.
- L1: `docs/modules/README.md#identity`.
- L2: `docs/05-contratos-api.md#8-integracion-con-el-microservicio-kyc-consumo`,
  `docs/02-arquitectura.md#9-adaptadores-externos-simulados`.

## Frontera
- Puede tocar: `backend/app/adapters/`.
- Prohibido: escribir tablas de `identity` o de otros modulos.

## Modelo de datos
No persiste.

## API / eventos
- Consume los 3 endpoints del KYC.
- Errores a propagar: `KYC_UNAVAILABLE`, `KYC_INVALID`, `KYC_TIMEOUT`.

## Reglas
- Timeout corto, 2 reintentos con backoff; circuit breaker ante caida.
- Config por variables de entorno (`KYC_BASE_URL`, `KYC_API_KEY`).

## CA
- `CA-01..CA-04` de HU01 (soporte).

## Pruebas
- Respuesta exitosa mock; timeout; caida con circuit breaker.

## Entregables
- [x] Interfaz + implementacion real + mock. [x] Pruebas. [x] Doc en `modules/README.md` si cambia.

## Entorno
Rama `feature/identity-kyc-adapter`. Pruebas: `<pytest>`.

## Reporte de cierre (§6 de `tasks/README.md`)

```
Tarea: E1-T01 - identity
Implementado: `KycProvider` (interfaz) + `HttpKycProvider` (httpx, timeout corto,
  2 reintentos con backoff exponencial, circuit breaker) + `MockKycProvider`
  (success/failure/timeout/down) + fabrica por env en `backend/app/adapters/kyc_provider.py`.
CA cubiertos: CA-01..CA-04 de HU01 (soporte) + pruebas del brief (mock exitoso, timeout,
  caida con circuit breaker) + key nunca expuesta; evidencia:
  `backend/tests/test_kyc_provider_adapter.py` (11 passed) y suite completa sin regresiones
  (208 passed, 1 skipped).
Decisiones/supuestos: rutas con prefijo `/api/v1` segun `05#8` (el brief las cita sin prefijo);
  `httpx` 0.28.1 ya presente en el .venv (sin urllib); `KYC_PROVIDER` default `mock` para
  local sin microservicio; 4xx -> `KYC_INVALID` sin reintento; red/5xx/timeout reintentan y
  luego `KYC_UNAVAILABLE`/`KYC_TIMEOUT`; circuito: 3 fallos consecutivos abren 30 s;
  logs solo con IDs/codigos (token solo como hash SHA-256 truncado); sin persistencia
  (sin tablas) y sin PII/frames.
Pendientes/riesgos: ninguno nuevo; `app/adapters/__init__.py` intacto (convivencia E1-T09);
  `modules/README.md` sin cambios (contrato ya descrito).
Archivos/endpoints/eventos afectados: NUEVO `backend/app/adapters/kyc_provider.py`;
  NUEVO `backend/tests/test_kyc_provider_adapter.py`; MOD `docs/tasks/E1-T01.md` (cierre).
  Consume `POST /api/v1/liveness/challenge`, `/api/v1/liveness/evaluate`,
  `/api/v1/identity/verify-full`. Sin eventos ni tablas.
```
