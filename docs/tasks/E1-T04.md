# E1-T04 - Persistir verificaciones KYC y auditoria

## Meta
| Campo | Valor |
|---|---|
| ID | `E1-T04` |
| Modulo | `identity` |
| Tipo | `datos` |
| Sprint | `1` |
| HU | `HU01` |
| Dependencias | `E1-T02` |
| Estado | `Hecho` |

## Objetivo
Guardar el resultado del KYC en `kyc_verifications` y registrar el evento en auditoria, sin
almacenar imagenes ni frames.

## Contexto embebido
- Solo se guarda el resultado (`overall_result`, `document_json`, `liveness_json`,
  `face_match_json`, hash del token de desafio).
- Prohibido persistir frames base64 o imagenes biometricas.
- La auditoria es obligatoria desde el Sprint 1 (aunque HU23 sea post-MVP).

## Context pack
- L0. L1: `docs/modules/README.md#identity` y `#audit`.
- L2: `docs/03b-diccionario-de-datos.md#44-kyc_verifications`,
  `docs/03b-diccionario-de-datos.md#141-audit_log-append-only`.

## Frontera
- Puede tocar: `identity/repository/`, y la fachada `audit.record(...)`.
- Prohibido: escribir `audit_log` directamente; usar la fachada.

## Modelo de datos
`kyc_verifications`; `audit_log` (via fachada).

## Reglas
- Un registro por intento, con motivo en caso de fallo (CA-04 de HU01).
- Retencion: no se borra.

## CA
- `CA-02` y `CA-04` de HU01.

## Pruebas
- Guardar exito y fallo; verificar que no hay campos de imagen.

## Entregables
- [x] Repositorio + integracion con auditoria. [x] Pruebas.

## Reporte (§6 de `docs/tasks/README.md`)
```
Tarea: E1-T04 - identity
Implementado: `save_verification(...)` guarda un registro por intento KYC en
  `identity.kyc_verifications` (exito/fallo con motivo) y audita via fachada
  `audit.record(...)` en la misma sesion; `audit_log` append-only con hash
  encadenado SHA-256 (genesis `prev_hash None`, `flush` sin `commit`).
CA cubiertos: CA-02 + CA-04 de HU01 + evidencia
  (`tests/test_kyc_verifications_audit.py`: 11 passed, 2 skipped sin Postgres;
  `tests/ -q`: 235 passed, 25 skipped, sin regresiones).
Decisiones/supuestos: `03b#4.4` no trae ningun campo de imagen (todo es
  resultado), asi que no hubo columna que excluir; se agrego guardia activa
  que rechaza claves frame/image/base64 y data-URIs. Desviaciones
  documentadas en codigo/migracion: `failure_reason TEXT` nulable (CA-04
  exige motivo y `03b#4.4` no trae columna), `seq` asignado por fachada
  `max+1` (BIGSERIAL en PG), `ip` texto portable (INET en PG),
  `ck_audit_log_actor_type` de refuerzo.
Pendientes/riesgos: ninguno nuevo; Parte C (Postgres) pendiente de entorno
  con BD (skips actuales).
Archivos/endpoints/eventos afectados:
  `backend/app/modules/identity/models/__init__.py` (KycVerification),
  `backend/app/modules/identity/repository/kyc_verifications.py` (nuevo) +
  re-export en `repository/__init__.py`,
  `backend/app/modules/audit/models/__init__.py` (AuditLog),
  `backend/app/modules/audit/repository/__init__.py` (lecturas),
  `backend/app/modules/audit/service/__init__.py` (fachada record),
  `backend/migrations/versions/0013_identity_kyc_audit.py` (nueva),
  `backend/tests/test_kyc_verifications_audit.py` (nuevo). Sin endpoints.
```

## Entorno
Rama `feature/identity-kyc-persistence`. Pruebas: `<pytest>`.
