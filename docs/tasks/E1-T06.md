# E1-T06 - Flutter: reintentos y mensajes de error de KYC

## Meta
| Campo | Valor |
|---|---|
| ID | `E1-T06` |
| Modulo | `frontend` |
| Tipo | `frontend` |
| Sprint | `1` |
| HU | `HU01` |
| Dependencias | `E1-T05` |
| Estado | `Hecho` |

## Objetivo
Manejar en la UI los reintentos limitados, la expiracion del token de desafio y la derivacion a
revision manual, con mensajes claros.

## Contexto embebido
- Token de desafio con TTL; al expirar, pedir uno nuevo.
- Maximo de reintentos configurable; si persiste, derivar a revision manual.
- `passed:false` no es error HTTP.

## Context pack
- L0. L1: `docs/13-frontend-flutter-panel.md#14-flujo-kyc-en-la-app-resumen-operativo`.
- L2: `docs/05-contratos-api.md#4-formato-de-respuesta`.

## Frontera
- Puede tocar: `frontend/`.
- Prohibido: `backend/`.

## Reglas
- Mapear `error.code` a mensajes accionables.
- No permitir avanzar de tarea sin exito del servidor.

## CA
- `CA-04` de HU01.

## Pruebas
- Token expirado; limite de reintentos; fallo persistente -> revision.

## Entregables
- [x] Manejo de errores + pantallas de estado. [x] Pruebas.

## Reporte (README seccion 6)

Tarea: `E1-T06` - `frontend`
Implementado: `KycErrorHandler` (error.code a mensaje accionable ES),
renovacion de challenge conservando progreso, limite configurable (default 3)
con derivacion a revision manual con folio, y vista de error con motivo del
servidor.
CA cubiertos: CA-04 HU01. Evidencia: 21 tests (handler + estado), tests E1-T05
intactos.
Decisiones/supuestos: `passed:false` = estado `TASK_NOT_PASSED` (no HTTP); el
servidor es autoridad (TTL local solo adelanta); folio local `KYC-...`.
Pendientes/riesgos: alinear folio con backend cuando exista endpoint de revision.
Archivos afectados: `lib/features/kyc/` (aditivo) + tests.

## Entorno
Rama `feature/frontend-kyc-errors`. `flutter test`.
