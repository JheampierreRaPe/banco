# E1-T08 - Servicio de OTP

## Meta
| Campo | Valor |
|---|---|
| ID | `E1-T08` |
| Modulo | `identity` |
| Tipo | `dominio` |
| Sprint | `1` |
| HU | `HU02` |
| Dependencias | `E1-T03` |
| Estado | `Pendiente` |

## Objetivo
Generar, almacenar (hash) y validar codigos OTP de un solo uso con expiracion, intentos y control
de reenvio.

## Contexto embebido
- OTP de un solo uso; expira a los 10 min; max 3 reenvios con espera minima.
- SLA de entrega < 5 s (depende de `E1-T09`).
- Se guarda solo el hash.

## Context pack
- L0. L1: `docs/modules/README.md#identity`.
- L2: `docs/03b-diccionario-de-datos.md#45-otp_codes`,
  `docs/06-epica-1-identidad-kyc.md#hu02---verificacion-y-activacion-de-cuenta-must-3-sp-sprint-1`.

## Frontera
- Puede tocar: `identity/service/`, `identity/repository/`.
- Prohibido: otros schemas.

## Modelo de datos
`otp_codes` (purpose, code_hash, expires_at, attempts, status).

## API / eventos
- Usado por `E1-T10`; emite `user.activated` al validar activacion.

## Reglas
- Codigo no reutilizable.
- Parametros `otp.ttl_seconds`, `otp.max_resends` desde `config.parameters`.

## CA
- `CA-01..CA-03` de HU02.

## Pruebas
- Generar/validar; expirado; reenvio; limite de intentos.

## Entregables
- [ ] Servicio + repositorio. [ ] Pruebas.

## Entorno
Rama `feature/identity-otp-service`. `<pytest>`.
