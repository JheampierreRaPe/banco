# E1-T16 - Flutter: biometria local, PIN de contingencia e inactividad

## Meta
| Campo | Valor |
|---|---|
| ID | `E1-T16` |
| Modulo | `frontend` |
| Tipo | `frontend` |
| Sprint | `1` |
| HU | `HU03` |
| Dependencias | `F-T02`, `F-T03`, `E1-T13` |
| Estado | `Hecho` |

## Objetivo
Pantalla de login que usa la biometria del dispositivo (Face ID/huella), con PIN de contingencia y
cierre automatico por inactividad.

## Contexto embebido
- El dispositivo no ejecuta liveness; usa `local_auth`.
- Firma el `nonce` con la clave guardada en almacenamiento seguro.
- Cerrar sesion tras `session.inactivity_seconds`.

## Context pack
- L0. L1: `docs/13-frontend-flutter-panel.md#14-flujo-kyc-en-la-app-resumen-operativo` (referencia de captura) y `#13-pantallas-por-modulo`.
- L2: `docs/13-frontend-flutter-panel.md#12-reglas-de-frontend`.

## Frontera
- Puede tocar: `frontend/`.
- Prohibido: `backend/`.

## API
- `POST /auth/login/challenge`, `/auth/login/facial`, `/auth/login/pin`.

## CA
- `CA-01`, `CA-02` y `CA-04` de HU03.

## Pruebas
- Biometria exitosa; fallback a PIN; inactividad cierra sesion.

## Entregables
- [x] Pantallas + integracion. [x] Pruebas de widget.

## Reporte (README seccion 6)

Tarea: `E1-T16` - `frontend`
Implementado: LoginPage (boton biometrico + PIN siempre visible + countdown de
inactividad 180s que limpia sesion) con controller delgado sobre F-T03;
`loginRoutes` reemplaza al placeholder /login al ensamblar.
CA cubiertos: CA-01, CA-02 y CA-04 HU03. Evidencia: 10 tests (bio/PIN/
inactividad/no-filtracion).
Decisiones/supuestos: PIN via `POST /auth/login/pin`; errores genericos
identicos; `local_auth` pendiente (hoy cae a PIN).
Pendientes/riesgos: agregar `local_auth` para biometria real.
Archivos afectados: `lib/features/login/` + tests.

## Entorno
Rama `feature/frontend-login`. `flutter test`.
