# E1-T17 - Auditoria de login e intentos fallidos

## Meta
| Campo | Valor |
|---|---|
| ID | `E1-T17` |
| Modulo | `identity` |
| Tipo | `datos` |
| Sprint | `1` |
| HU | `HU03` |
| Dependencias | `E1-T13`, `E1-T14` |
| Estado | `Hecho` |

## Objetivo
Registrar en auditoria cada login exitoso y cada intento fallido, con dispositivo, IP y fecha.

## Contexto embebido
- La auditoria es obligatoria desde el Sprint 1 (HU23 sera post-MVP, pero el log basico no).
- No incluir PII en logs; usar IDs.

## Context pack
- L0. L1: `docs/modules/README.md#audit`.
- L2: `docs/03b-diccionario-de-datos.md#141-audit_log-append-only`.

## Frontera
- Puede tocar: llamada a la fachada `audit.record(...)` desde `identity/service/`.
- Prohibido: escribir `audit_log` directamente.

## CA
- Soporte a `CA-03` de HU03 y base de HU23.

## Pruebas
- Se registra login exitoso y fallido; cadena de hash valida (stub).

## Entregables
- [x] Integracion con auditoria. [x] Pruebas.

## Reporte (§6 docs/tasks/README.md)
```
Tarea: E1-T17 - identity
Implementado: login facial/PIN/refresh audita auth.login_succeeded en exito y auth.failed_attempt en fallo via fachada audit.record (misma sesion, flush sin commit, best-effort); sin PII (solo IDs + method/at/reason).
CA cubiertos: CA-03 de HU03 + base HU23. Evidencia: backend/tests/test_auth_audit.py (8 passed) con cadena verify_chain valida, filas sin PIN/firmas/nonces/tokens, y best-effort que no rompe el login; sin regresion en test_device_login.py (10 passed), test_pin_login.py (10 passed) y test_sessions.py (13 passed).
Decisiones/supuestos: best-effort con try/except (no estricto) como E1-T03: la auditoria es observabilidad y nunca debe romper la disponibilidad del login; coherente con modules/README.md#audit (prohibido bloquear el flujo). verify_chain ya existia en audit/service (puro, sobre lista ordenada por seq): no se agrego nada en audit. Persistencia del failed_attempt en fallos HTTP depende del commit del endpoint llamante (pin confirma fallos; device/sessions heredan su comportamiento).
Pendientes/riesgos: ninguno nuevo; cadena auditada en SQLite/pytest, migracion 0013 ya cubre audit_log en Postgres.
Archivos/endpoints/eventos afectados: backend/app/modules/identity/service/device_login.py, pin_login.py, sessions.py (solo instrumentacion aditiva: _audit_auth_event + llamadas; logica intacta); backend/tests/test_auth_audit.py (nuevo); eventos auth.login_succeeded / auth.failed_attempt en audit_log (entity=user).
```

## Entorno
Rama `feature/identity-auth-audit`. `<pytest>`.
