# E2-T02 - Endpoints de cuentas y detalle de saldos

## Meta
| Campo | Valor |
|---|---|
| ID | `E2-T02` |
| Modulo | `accounts` |
| Tipo | `dominio` |
| Sprint | `1` |
| HU | `HU05` |
| Dependencias | `E2-T01` |
| Estado | `Hecho` |

## Objetivo
Exponer `/accounts` (consolidado) y `/accounts/{id}` (detalle con disponible, retenido y
contable), con enmascaramiento del numero de cuenta.

## Contexto embebido
- Numero de cuenta siempre enmascarado.
- `contable = disponible + retenido`.
- El cliente solo ve sus propias cuentas (RBAC).

## Context pack
- L0. L1: `docs/modules/README.md#accounts`.
- L2: `docs/05-contratos-api.md#62-cuentas-y-beneficiarios-accounts---hu05-hu07`,
  `docs/03b-diccionario-de-datos.md#51-accounts`.

## Frontera
- Puede tocar: `accounts/api/`, `schemas/`, `service/`.
- Prohibido: calcular saldos por fuera de `account_balances`.

## API
- `GET /accounts`, `GET /accounts/{id}`.

## CA
- `CA-01` y `CA-02` de HU05.

## Pruebas
- Cliente con multiples cuentas; enmascaramiento; 403 si la cuenta no es propia.

## Entregables
- [x] Endpoints + esquemas + OpenAPI. [x] Pruebas.

## Reporte de cierre (§6 de docs/tasks/README.md)
```
Tarea: E2-T02 - accounts
Implementado: GET /api/v1/accounts (consolidado) y GET /api/v1/accounts/{id}
  (detalle con disponible/retenido/contable) con numero enmascarado ****1234.
CA cubiertos: CA-01 y CA-02 de HU05 + evidencia (tests/test_accounts_endpoints.py,
  7/7 verde: consolidado multi-cuenta, enmascaramiento, 403 ajena, 404 inexistente,
  401 sin auth, lista solo propia; regresion tests/test_accounts_repository.py 15/15).
Decisiones/supuestos: se reutiliza core.security.decode_token (JWT Bearer, sub=user_id);
  service/ filtra por user_id y verifica propiedad (403) / existencia (404); contable =
  available + held leido de account_balances (sin calculo externo); envoltorio
  {"data","meta"} segun 05#4; el router se monta solo via iter_routers (sin cambio en
  app/main.py); tests inyectan auth falsa con dependency_overrides[get_current_user_id].
Pendientes/riesgos: ninguno bloqueante; E2-T04 corre en paralelo sobre repository/ y
  migraciones (no tocados aqui). Si el repositorio exponga paginacion/filtros a futuro,
  el service ya aisla ese cambio del router.
Archivos/endpoints/eventos afectados: backend/app/modules/accounts/api/__init__.py,
  schemas/__init__.py, service/__init__.py, tests/test_accounts_endpoints.py (nuevo),
  backend/app/modules/accounts/README.md; endpoints GET /accounts, GET /accounts/{id};
  sin eventos nuevos.
```

## Entorno
Rama `feature/accounts-endpoints`. `<pytest>`.
