# E2-T04 - Proyeccion de movimientos (movements_view)

## Meta
| Campo | Valor |
|---|---|
| ID | `E2-T04` |
| Modulo | `accounts` |
| Tipo | `datos` |
| Sprint | `1` |
| HU | `HU05` |
| Dependencias | `E5-T10` |
| Estado | `Pendiente` |

## Objetivo
Construir y mantener `movements_view` a partir de los asientos publicados (`ledger.entry.posted`).

## Contexto embebido
- La fuente de verdad es `ledger.postings`; la vista es proyeccion de lectura.
- Consumo idempotente de eventos (registrar en `processed_events`).
- Si la proyeccion difiere del origen, gana el origen.

## Context pack
- L0. L1: `docs/modules/README.md#accounts` y `#ledger`.
- L2: `docs/03c-modelo-er.md#14-fuente-de-verdad-y-proyecciones`,
  `docs/04-motor-transaccional-y-ledger.md#2-contabilidad-de-partida-doble-obligatoria`.

## Frontera
- Puede tocar: `accounts/repository/`, migraciones del schema `accounts`.
- Prohibido: leer `ledger` por FK; consumir solo por evento o fachada.

## Modelo de datos
`movements_view`.

## API / eventos
- Consume `ledger.entry.posted`.

## CA
- `CA-03` de HU05.

## Pruebas
- Evento genera fila; reproceso no duplica (idempotencia).

## Entregables
- [ ] Vista/consulta + consumidor. [ ] Pruebas.

## Entorno
Rama `feature/accounts-movements-projection`. `<pytest>`.
