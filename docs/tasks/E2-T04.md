# E2-T04 - Proyeccion de movimientos (movements_view)

## Meta
| Campo | Valor |
|---|---|
| ID | `E2-T04` |
| Modulo | `accounts` |
| Tipo | `datos` |
| Sprint | `1` |
| HU | `HU05` |
| Dependencias | `E5-T10` |
| Estado | `Hecho` |

## Objetivo
Construir y mantener `movements_view` a partir de los asientos publicados (`ledger.entry.posted`).

## Contexto embebido
- La fuente de verdad es `ledger.postings`; la vista es proyeccion de lectura.
- Consumo idempotente de eventos (registrar en `processed_events`).
- Si la proyeccion difiere del origen, gana el origen.

## Context pack
- L0. L1: `docs/modules/README.md#accounts` y `#ledger`.
- L2: `docs/03c-modelo-er.md#14-fuente-de-verdad-y-proyecciones`,
  `docs/04-motor-transaccional-y-ledger.md#2-contabilidad-de-partida-doble-obligatoria`.

## Frontera
- Puede tocar: `accounts/repository/`, migraciones del schema `accounts`.
- Prohibido: leer `ledger` por FK; consumir solo por evento o fachada.

## Modelo de datos
`movements_view`.

## API / eventos
- Consume `ledger.entry.posted`.

## CA
- `CA-03` de HU05.

## Pruebas
- Evento genera fila; reproceso no duplica (idempotencia).

## Entregables
- [x] Vista/consulta + consumidor. [x] Pruebas.

## Entorno
Rama `feature/accounts-movements-projection`. `<pytest>`.

## Cierre (reporte §6 de docs/tasks/README.md)
```
Tarea: E2-T04 - accounts
Implementado: proyeccion movements_view (tabla con columnas exactas 03b#5.4) +
  consumidor idempotente handle_movement_event de ledger.entry.posted +
  rebuild_entry_movements (gana el origen) + migracion 0010 + 11 pruebas A/B/C.
CA cubiertos: CA-03 de HU05 + evidencia (pruebas)
  - Evento genera filas correctas: test_event_projects_rows_with_amount_date_direction_account
  - Reproceso no duplica: test_reprocessing_does_not_duplicate (mismo event_id y
    re-entrega con distinto event_id por UQ natural)
  - Refresh reconstruye tras borrado manual: test_refresh_rebuilds_after_manual_delete
Decisiones/supuestos:
  - Tabla de proyeccion (no MATERIALIZED VIEW nativa): la vista nativa exigiria
    SELECT sobre ledger.postings (JOIN entre modulos, prohibido regla de oro 4);
    el refresh es re-derivacion desde postings entregados por evento/fachada.
  - Mapeo cuenta: 1) posting.account_ref (via primaria), 2) fallback
    ledger_account_id contra REFs propias (ledger_account_id/hold), 3) cuentas
    del sistema sin dueno se omiten. Sin importar modelos ledger.
  - UQ natural = PK compuesta (journal_entry_id, account_id, direction,
    amount_minor, currency); consumidor "accounts-movements" en processed_events.
Pendientes/riesgos: ninguno nuevo (no se registra en docs/17).
Archivos/endpoints/eventos afectados:
  - backend/app/modules/accounts/repository/movements.py (NUEVO)
  - backend/app/modules/accounts/repository/__init__.py (solo re-export E2-T04)
  - backend/migrations/versions/0010_accounts_movements.py (NUEVA, down 0009)
  - backend/tests/test_accounts_movements.py (NUEVO, 11 pruebas)
  - Evento consumido: ledger.entry.posted. Sin endpoints nuevos.
```
