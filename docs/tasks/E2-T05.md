# E2-T05 - Flutter: dashboard, detalle y filtros

## Meta
| Campo | Valor |
|---|---|
| ID | `E2-T05` |
| Modulo | `frontend` |
| Tipo | `frontend` |
| Sprint | `1` |
| HU | `HU05` |
| Dependencias | `F-T01`, `E2-T02`, `E2-T03` |
| Estado | `Hecho` |

## Objetivo
Pantallas de dashboard (cuentas y saldos), detalle de cuenta, lista de movimientos y exportacion.

## Contexto embebido
- Mostrar disponible, retenido y contable de forma diferenciada.
- Numero de cuenta enmascarado.
- Estados: cargando, vacio, error, contenido.

## Context pack
- L0. L1: `docs/13-frontend-flutter-panel.md#13-pantallas-por-modulo`.
- L2: `docs/05-contratos-api.md#62-cuentas-y-beneficiarios-accounts---hu05-hu07`.

## Frontera
- Puede tocar: `frontend/`.
- Prohibido: `backend/`.

## API
- `GET /accounts`, `/accounts/{id}`, `/accounts/{id}/movements`, `/movements/export`.

## CA
- `CA-01..CA-04` de HU05.

## Pruebas
- Render de cuentas; detalle; filtro; exportacion.

## Entregables
- [x] Pantallas + integracion. [x] Pruebas de widget.

## Reporte (README seccion 6)

Tarea: `E2-T05` - `frontend`
Implementado: Dashboard (consolidado con pull-to-refresh) + detalle de cuenta
con movimientos paginados, filtros fecha/direccion y export CSV (retorna bytes
+ nombre sugerido, sin `file_saver`); numero siempre enmascarado.
CA cubiertos: CA-01..CA-04 HU05. Evidencia: 8 tests.
Decisiones/supuestos: solo GET (sin Idempotency-Key); parseo defensivo de
montos (nunca se inventa dinero); guardado nativo pendiente (F-futuro).
Pendientes/riesgos: filtros en memoria (deuda R21 backend).
Archivos afectados: `lib/features/accounts/` + tests.

## Entorno
Rama `feature/frontend-accounts`. `flutter test`.
