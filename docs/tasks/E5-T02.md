# E5-T02 - Persistencia del motor: transactions, historial y holds

## Meta
| Campo | Valor |
|---|---|
| ID | `E5-T02` |
| Modulo | `transactions` |
| Tipo | `datos` |
| Sprint | `1` |
| HU | `HU17` |
| Dependencias | `E5-T01`, `E2-T01` |
| Estado | `Pendiente` |

## Objetivo
Implementar modelos, migraciones y repositorios de `transactions`, `transaction_status_history`
y `holds`.

## Contexto embebido
- Dinero en centimos; `amount_minor > 0`.
- Estados segun `E5-T01`; `holds` soporta `ACTIVE/RELEASED/CAPTURED/EXPIRED`.
- `idempotency_key` indexado.

## Context pack
- L0. L1: `docs/modules/README.md#transactions`.
- L2: `docs/03b-diccionario-de-datos.md#6-schema-transactions`.

## Frontera
- Puede tocar: `transactions/models/`, `repository/`, migraciones del schema.
- Prohibido: schemas `ledger`, `accounts`.

## API
- Sin endpoints (capa de datos).

## CA
- Base de `CA-01..CA-04` de HU17.

## Pruebas
- CRUD; historial por transicion; hold con estado y expiracion.

## Entregables
- [ ] Modelos + repositorio + migracion. [ ] Pruebas.

## Entorno
Rama `feature/transactions-persistence`. `<pytest>`.
