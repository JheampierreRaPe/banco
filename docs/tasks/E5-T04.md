# E5-T04 - Idempotencia (Idempotency-Key)

## Meta
| Campo | Valor |
|---|---|
| ID | `E5-T04` |
| Modulo | `transactions` |
| Tipo | `motor-ledger` |
| Sprint | `1` |
| HU | `HU17` |
| Dependencias | `E5-T02` |
| Estado | `Hecho` |

## Objetivo
Implementar el almacenamiento y el middleware de idempotencia para todos los endpoints que mueven
dinero.

## Contexto embebido
- Misma `Idempotency-Key` + mismo cuerpo = misma respuesta, sin doble debito.
- Misma clave con cuerpo distinto = `409`.
- TTL configurable (p. ej. 24 h).
- Obligatoria en endpoints de dinero.

## Context pack
- L0. L1: `docs/modules/README.md#transactions`.
- L2: `docs/04-motor-transaccional-y-ledger.md#5-idempotencia`,
  `docs/03b-diccionario-de-datos.md#21-idempotency_keys`.

## Frontera
- Puede tocar: `shared/` (tabla) y middleware/`transactions/api/`.
- Prohibido: otros modulos.

## Modelo de datos
`idempotency_keys`.

## Reglas
- Guardar hash del cuerpo y snapshot de respuesta.
- Si la solicitud no mueve dinero, la cabecera es opcional.

## CA
- `CA-03` de HU17.

## Pruebas
- Repeticion exacta; clave con cuerpo distinto; expiracion.

## Entregables
- [x] Middleware + repositorio. [x] Pruebas.

## Reporte (§6 de `docs/tasks/README.md`)

```
Tarea: E5-T04 - transactions
Implementado: Idempotencia Idempotency-Key (HU17 CA-03): modelo
  `shared.idempotency_keys` (03b§2.1) + repositorio con claim atomico por
  UQ + middleware en `transactions/api` que exige la cabecera en dinero
  (replay exacto sin re-ejecutar, 409 con distinto cuerpo, renovacion al
  expirar, TTL por env default 24h).
CA cubiertos: CA-03 de HU17 + evidencia (pruebas):
  `backend/tests/test_transactions_idempotency.py` 12/12 verde
  (repeticion exacta con contador==1, 409, expiracion, doble claim
  concurrente sin duplicar); `test_shared_outbox.py` sigue verde (15/15).
Decisiones/supuestos: solo aditivo sobre E5-T05 (OutboxEntry/
  ProcessedEvent y `outbox.py` intactos); `flush` sin `commit` (la
  transaccion la decide el llamante); hash SHA-256 de cuerpo canonico
  JSON; TTL lee `IDEMPOTENCY_TTL_SECONDS` (default 86400; E2/fase config
  podra moverlo a `parameters`); sin `float`, sin publicar eventos, sin
  tocar `transactions/service` ni `jobs/`; migracion 0007 declara
  `down_revision="0006_ledger_balances"`.
Pendientes/riesgos: con `user_id` NULL (servicios tecnicos) la UQ no
  deduplica (semantica NULL en Postgres/SQLite); evaluar indice unico
  parcial en follow-up. Cableado del middleware a routers concretos
  cuando E5-T02 exponga los endpoints de dinero.
Archivos/endpoints/eventos afectados:
  - `backend/app/modules/shared/models/__init__.py` (aditivo: `IdempotencyKey`)
  - `backend/app/modules/shared/repository/idempotency.py` (nuevo)
  - `backend/app/modules/shared/repository/__init__.py` (re-export)
  - `backend/app/modules/transactions/api/idempotency.py` (nuevo; sin eventos)
  - `backend/migrations/versions/0007_shared_idempotency.py` (nueva)
  - `backend/tests/test_transactions_idempotency.py` (nuevo)
```


## Entorno
Rama `feature/transactions-idempotency`. `<pytest>`.
