# E5-T11 - Validador de cuadre contable

## Meta
| Campo | Valor |
|---|---|
| ID | `E5-T11` |
| Modulo | `ledger` |
| Tipo | `motor-ledger` |
| Sprint | `1` |
| HU | `HU18` |
| Dependencias | `E5-T10` |
| Estado | `Hecho` |

## Objetivo
Implementar la validacion que impide confirmar un asiento cuyos debitos y creditos no cuadren.

## Contexto embebido
- Regla de oro: `sum(debitos) = sum(creditos)` por moneda.
- Si no cuadra, el asiento no se crea y se lanza error.
- Validacion tambien en el job de cierre.

## Context pack
- L0. L1: `docs/modules/README.md#ledger`.
- L2: `docs/04-motor-transaccional-y-ledger.md#2-contabilidad-de-partida-doble-obligatoria`.

## Frontera
- Puede tocar: `ledger/domain/` (validador).
- Prohibido: saltarse la validacion en cualquier ruta.

## Reglas
- Enteros en centimos; sin redondeo que descuadre.
- La validacion es parte del agregado (no se puede desactivar).

## CA
- `CA-01` y `CA-03` de HU18.

## Pruebas
- Cuadra; descuadra por 1 centimo; multi-moneda (cada asiento cuadra en su moneda).

## Entregables
- [x] Validador + pruebas.

## Reporte (README seccion 6)
```
Tarea: E5-T11 - ledger
Implementado: validador de cuadre `validate_balanced` como pieza explicita del agregado (sum DEBIT = sum CREDIT por moneda, enteros en centimos, sin flags/bypass) y pruebas A/B/C en `test_ledger_balance_validator.py`.
CA cubiertos: CA-01 + evidencia (asiento que cuadra aceptado en dominio/SQLite/Postgres; descuadre por 1 centimo rechazado sin filas residuales); CA-03 + evidencia (multi-moneda: cada asiento cuadra en su moneda, mezcla PEN/USD rechazada). Verificacion: `.\.venv\Scripts\python.exe -m pytest tests/test_ledger_balance_validator.py tests/test_ledger_double_entry.py tests/test_ledger_chart_of_accounts.py -v` -> 35 passed.
Decisiones/supuestos: E5-T10 ya validaba dentro de `post_entry` via `validate_postings`; E5-T11 explicita `validate_balanced` y hace que `validate_postings` delegue en ella siempre. Auditoria de rutas: unica creacion en `repository/entries.py::post_entry` (tras validar) + `reverse_entry` que delega + fachada `service` que delega; sin bypass, sin `float`/`round`, sin eventos (regla de oro 8), flush sin commit.
Pendientes/riesgos: ninguno; validacion tambien exigible en el job de cierre (fuera de alcance de este brief, corresponde a la tarea de cierre diario).
Archivos/endpoints/eventos afectados: `backend/app/modules/ledger/domain/entries.py` (nuevo `validate_balanced`, `validate_postings` delega), `backend/app/modules/ledger/repository/entries.py` (docstring E5-T11, sin cambio de logica), `backend/tests/test_ledger_balance_validator.py` (nuevo, 9 pruebas). Sin endpoints ni eventos.
```

## Entorno
Rama `feature/ledger-balance-validator`. `<pytest>`.
