# E5-T12 - Actualizacion atomica de proyecciones de saldo

## Meta
| Campo | Valor |
|---|---|
| ID | `E5-T12` |
| Modulo | `ledger` |
| Tipo | `motor-ledger` |
| Sprint | `1` |
| HU | `HU18` |
| Dependencias | `E5-T10`, `E2-T01` |
| Estado | `Hecho` |

## Objetivo
Actualizar `ledger_balances` y `account_balances` en la misma transaccion que el asiento.

## Contexto embebido
- Las proyecciones no son fuente de verdad; gana `postings`.
- Se actualizan de forma atomica con el asiento.
- Versionado optimista.

## Context pack
- L0. L1: `docs/modules/README.md#ledger` y `#accounts`.
- L2: `docs/03c-modelo-er.md#14-fuente-de-verdad-y-proyecciones`.

## Frontera
- Puede tocar: `ledger/repository/`; llamar a la fachada de `accounts` para la proyeccion.
- Prohibido: otros accesos directos.

## Modelo de datos
`ledger_balances`, `account_balances`.

## CA
- `CA-04` de HU18 (control de consistencia).

## Pruebas
- Saldo sube/baja correctamente; version incrementa; rollback deja proyeccion intacta.

## Entregables
- [x] Actualizacion atomica + pruebas.

## Reporte (§6 `docs/tasks/README.md`)
```
Tarea: E5-T12 - ledger
Implementado: proyeccion atomica `ledger_balances` con optimistic locking
  (`post_entry_and_update_balances` reutiliza `post_entry`, misma
  transaccion) + chequeo `sum(postings)==balances` (base E5-T13).
CA cubiertos: CA-04 de HU18 + evidencia (pruebas)
  - `tests/test_ledger_balances.py`: 12/12 (SQLite A/B + Postgres C).
  - Verificacion: 38 passed
    (`test_ledger_balances` + `test_ledger_double_entry` +
    `test_ledger_chart_of_accounts`, `.venv` propio, Postgres disponible).
Decisiones/supuestos: delta firmado DEBIT + / CREDIT - (solo `int`, sin
  `float`); fuente de verdad `postings` (gana ante divergencia, `03c#14`);
  `flush` sin `commit`; sin eventos (E5-T05); migracion 0006 declara
  `down_revision="0005_shared_outbox"` por convivencia (cadena lineal al
  aterrizar ambos); solo schema `ledger`.
Pendientes/riesgos: E2-T01 (fase 4): tabla `account_balances` + adaptador
  real de `AccountProjectionPort` (hoy stub `NotImplementedError` omitido
  silenciosamente; tests usan fake).
Archivos/endpoints/eventos afectados:
  - `backend/app/modules/ledger/models/__init__.py` (aditivo: `LedgerBalance`)
  - `backend/app/modules/ledger/repository/balances.py` (nuevo)
  - `backend/app/modules/ledger/repository/__init__.py` (re-export)
  - `backend/migrations/versions/0006_ledger_balances.py` (nueva)
  - `backend/tests/test_ledger_balances.py` (nuevo)
  - Sin endpoints nuevos; sin eventos publicados.
```

## Entorno
Rama `feature/ledger-balances`. `<pytest>`.
