# E5-T12 - Actualizacion atomica de proyecciones de saldo

## Meta
| Campo | Valor |
|---|---|
| ID | `E5-T12` |
| Modulo | `ledger` |
| Tipo | `motor-ledger` |
| Sprint | `1` |
| HU | `HU18` |
| Dependencias | `E5-T10`, `E2-T01` |
| Estado | `Pendiente` |

## Objetivo
Actualizar `ledger_balances` y `account_balances` en la misma transaccion que el asiento.

## Contexto embebido
- Las proyecciones no son fuente de verdad; gana `postings`.
- Se actualizan de forma atomica con el asiento.
- Versionado optimista.

## Context pack
- L0. L1: `docs/modules/README.md#ledger` y `#accounts`.
- L2: `docs/03c-modelo-er.md#14-fuente-de-verdad-y-proyecciones`.

## Frontera
- Puede tocar: `ledger/repository/`; llamar a la fachada de `accounts` para la proyeccion.
- Prohibido: otros accesos directos.

## Modelo de datos
`ledger_balances`, `account_balances`.

## CA
- `CA-04` de HU18 (control de consistencia).

## Pruebas
- Saldo sube/baja correctamente; version incrementa; rollback deja proyeccion intacta.

## Entregables
- [ ] Actualizacion atomica + pruebas.

## Entorno
Rama `feature/ledger-balances`. `<pytest>`.
