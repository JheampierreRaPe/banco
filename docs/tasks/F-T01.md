# F-T01 - Base del proyecto Flutter, tema y navegacion

## Meta

| Campo | Valor |
|---|---|
| ID | `F-T01` |
| Modulo | `frontend` |
| Tipo | `frontend` |
| Sprint | `1` |
| HU relacionadas | `Transversal` |
| Prioridad | `Must` |
| Estado | `Pendiente` |
| Dependencias | `Ninguna` |

## Objetivo

Dejar la estructura base de la app Flutter: tema, navegacion con guardas de sesion, capa HTTP con
interceptores y manejo estandar de errores, sin logica de negocio.

## Alcance

- **Incluye:** proyecto, tema, `go_router` con guardas, cliente `dio`, manejo de errores y estados
  de carga/vacio.
- **No incluye:** pantallas de negocio (KYC, cuentas, pagos).

## Contexto embebido (decisiones que aplican)

- El dispositivo **no** ejecuta modelos de IA (no incluir analisis de imagen local).
- Toda accion que mueve dinero usa `Idempotency-Key`.
- Nunca embeber API keys de servicios en la app.
- Datos sensibles enmascarados por defecto.

## Context pack (leer solo esto)

- L0: este brief.
- L1: `docs/modules/README.md` -> `frontend` no existe como backend; usar `docs/13`.
- L2:
  - `docs/13-frontend-flutter-panel.md#1-app-flutter-cliente-y-comercio`
  - `docs/13-frontend-flutter-panel.md#12-reglas-de-frontend`
  - `docs/05-contratos-api.md#1-convenciones-generales`
  - `docs/05-contratos-api.md#4-formato-de-respuesta`
- Prohibido leer: `docs/03`, `docs/04` (no aplican al frontend).

## Frontera de codigo

- **Puede tocar:** `frontend/`.
- **Prohibido tocar:** `backend/`, `admin-web/`.

## Modelo de datos

No aplica.

## API / eventos

- **Consume:** contrato general (`05`); aun sin endpoints de negocio.
- **Errores de negocio:** mapear el formato `error.code`/`error.message` de `05` a mensajes de UI.

## Reglas de negocio

- Los tokenes se guardan en `flutter_secure_storage`.
- El interceptador agrega `Authorization`, `X-Request-Id` e `Idempotency-Key` cuando corresponde.
- Toda pantalla tiene estados: cargando, vacio, error, contenido.

## Criterios de aceptacion (CA)

- Base tecnica lista para que las demas tareas de UI se implementen sin reescribir navegacion.

## Pruebas requeridas

- Navegacion a login sin sesion redirige correctamente.
- Interceptores agregan cabeceras.
- Manejo de error de red muestra mensaje.

## Entregables

- [ ] Proyecto Flutter base + tema.
- [ ] Navegacion con guardas.
- [ ] Capa HTTP + manejo de errores.
- [ ] Pruebas de widget basicas.

## Entorno / comandos

- Rama: `feature/frontend-base`
- Ejecutar: `flutter run`
- Pruebas: `flutter test`

## Reporte esperado

Seguir el formato de `docs/tasks/README.md` seccion 6.
