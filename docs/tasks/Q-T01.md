# Q-T01 - Esqueleto de pruebas y datos semilla

## Meta
| Campo | Valor |
|---|---|
| ID | `Q-T01` |
| Modulo | `qa` |
| Tipo | `qa` |
| Sprint | `1` |
| HU | `Transversal` |
| Dependencias | `Ninguna` |
| Estado | `Hecho` |

## Objetivo
Armar la infraestructura de pruebas (fixtures, base de datos de prueba, factories) y el script de
datos semilla reproducible.

## Contexto embebido
- Nunca usar datos personales reales; todo simulado.
- Semilla: usuarios demo, cuentas PEN/USD, saldos, catalogo contable, comercios, billers.
- Scripts versionados y reproducibles.

## Context pack
- L0. L1: `docs/modules/README.md#shared--config-infraestructura`.
- L2: `docs/15-calidad-seguridad-devops.md#12-datos-de-prueba`.

## Frontera
- Puede tocar: `backend/tests/`, scripts de seed.
- Prohibido: codigo de produccion.

## Reglas
- Cada prueba arranca en estado conocido.
- La semilla debe poder recrearse en cualquier entorno.

## CA
- Habilita todas las demas pruebas (transversal).

## Pruebas
- Levantar DB de prueba; cargar semilla; test de humo.

## Entregables
- [x] Fixtures + factories + seed. [x] Documentacion en `15`.

## Reporte (feature/qa-bootstrap)
- `backend/tests/`: `conftest.py` (fixtures `client/test_engine/db_session/db_client/seeded_snapshot`),
  `factories.py`, `db_utils.py`, `seed.py`, `test_factories.py` (8), `test_seed_smoke.py` (6),
  `data/seed_demo.json` (snapshot canonico, fingerprint `827d0047…`).
- `backend/scripts/seed_demo.py`: `check|dump|load` (crea `banca_test`, migra, carga idempotente
  en `qa.seed_snapshots`, verifica 13 esquemas + 13 parametros).
- Verificacion: `seed_demo.py load` OK + `pytest` 15 passed + `ruff`/`black` limpios.
- Sin tocar codigo de produccion (`app/`, `migrations/` intactos).

## Entorno
Rama `feature/qa-bootstrap`. `<pytest>`.
