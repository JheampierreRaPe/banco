# Q-T02 - Suite de pruebas del motor transaccional

## Meta

| Campo | Valor |
|---|---|
| ID | `Q-T02` |
| Modulo | `qa` |
| Tipo | `qa` |
| Sprint | `1` (y regresion continua) |
| HU relacionadas | `HU17`, `HU18` |
| Prioridad | `Must` |
| Estado | `Pendiente` |
| Dependencias | `E5-T03`, `E5-T09`, `E5-T10` |

## Objetivo

Automatizar los 12 casos obligatorios del motor y del ledger, incluyendo idempotencia,
concurrencia, reversos y cuadre, como condicion para cerrar HU17/HU18.

## Alcance

- **Incluye:** los casos de la seccion 12 de `docs/04`.
- **No incluye:** pruebas de UI ni de adaptadores externos.

## Contexto embebido (decisiones que aplican)

- `sum(debitos) = sum(creditos)` por asiento y moneda.
- Un `FAILED` siempre libera holds.
- Peticion duplicada con misma `Idempotency-Key` = un solo debito.
- Dos debitos concurrentes no producen sobregiro.
- `postings` y `journal_entries` son append-only.

## Context pack (leer solo esto)

- L0: este brief.
- L1: `docs/modules/README.md` -> `transactions` y `ledger`.
- L2:
  - `docs/04-motor-transaccional-y-ledger.md#12-pruebas-obligatorias-del-motor`
  - `docs/15-calidad-seguridad-devops.md#11-casos-de-prueba-obligatorios-del-motor`

## Frontera de codigo

- **Puede tocar:** `backend/tests/`.
- **Prohibido tocar:** codigo de produccion de cualquier modulo (solo lectura; si hay bug, se
  reporta y se corrige via tarea del modulo).

## Modelo de datos

Usa los schemas `transactions` y `ledger` con datos semilla.

## API / eventos

- **Endpoints:** los del motor (`/transfers/*`, `/transactions/*`) para pruebas de integracion.
- Validar tambien la emision de eventos por `outbox`.

## Reglas de negocio

Fijadas por `docs/04`. Si una prueba revela una regla ambigua, se registra en `docs/17`.

## Criterios de aceptacion (CA)

- `CA-03` de HU17 y `CA-01..CA-04` de HU18 cubiertos por pruebas verdes.

## Pruebas requeridas

1. Transferencia feliz propia y a tercero.
2. Saldo insuficiente rechazado sin cambios.
3. Doble peticion idempotente.
4. Debitos concurrentes sin sobregiro.
5. Reverso con original intacto.
6. Cuadre por asiento.
7. Cierre diario cuadrado.
8. Interbancario exito/timeout/compensacion.
9. Atomicidad ante fallo parcial.
10. Hold vencido liberado por job.
11. UUID unico por asiento.
12. Latencia del camino de escritura.

## Entregables

- [ ] Suite automatizada del motor y ledger.
- [ ] Reporte de cobertura de los 12 casos.
- [ ] Registro de hallazgos en `docs/17` si aplica.

## Entorno / comandos

- Rama: `feature/qa-motor-suite`
- Pruebas: `<comando de pytest>`

## Reporte esperado

Seguir el formato de `docs/tasks/README.md` seccion 6.
