# Q-T05 - Pipeline CI

## Meta
| Campo | Valor |
|---|---|
| ID | `Q-T05` |
| Modulo | `devops` |
| Tipo | `devops` |
| Sprint | `1` |
| HU | `Transversal` |
| Dependencias | `Q-T01` |
| Estado | `Hecho` |

## Objetivo
Configurar GitHub Actions con lint, pruebas, validacion de migraciones y build de imagenes.

## Contexto embebido
- Rama `main` protegida; PR obligatorio con CI verde.
- Sin secretos en el repositorio.
- Validar migraciones `up/down` en base limpia.

## Context pack
- L0. L1: `docs/modules/README.md#shared--config-infraestructura`.
- L2: `docs/15-calidad-seguridad-devops.md#4-cicd`.

## Frontera
- Puede tocar: `.github/workflows/`, `infra/ci/`.
- Prohibido: codigo de negocio.

## Reglas
- Lint (`ruff`, `black`), pruebas, migraciones, build, escaneo de dependencias.
- Fallar el pipeline ante cualquier error.

## CA
- Transversal a todas las HU del sprint.

## Pruebas
- Pipeline verde en una rama de ejemplo; rojo ante un test fallido.

## Entregables
- [x] Workflows de CI. [x] Documentacion de uso.

## Reporte (feature/ci-pipeline)
- `.github/workflows/ci.yml`: jobs `lint` (ruff + black + secretos), `test` (pytest con
  Postgres 16/Redis como servicios), `migrations` (up/down/up en `ci_migrations` limpia),
  `build` (imagenes backend + worker), `scan` (pip-audit + gitleaks) y `ci-success`
  (agregador para proteccion de `main`). Sin `continue-on-error`: todo error tiñe de rojo.
- `infra/ci/`: `check-migrations.sh`, `secrets-check.sh`, `run-local.sh`/`run-local.ps1`
  (paridad local), `Dockerfile.backend`/`Dockerfile.worker` (verificacion de build; la
  imagen demo final es de Q-T06), `README.md` (uso, proteccion de rama, secretos),
  `.gitattributes` (LF forzado en `.sh`).
- Verificacion local: `ruff`/`black` limpios, `pytest` 15 passed, `check-migrations.sh`
  OK (up/down/up), `docker build` de ambas imagenes OK + humo (`/health` -> `ok`),
  `pip-audit` sin vulnerabilidades (se actualiza `setuptools>=83` antes de auditar, por
  PYSEC-2026-3447 en toolchains antiguos), `secrets-check.sh` OK.
- Rojo ante fallo: prueba temporal con `assert False` -> `pytest` exit 1 (eliminada
  despues); patron de secretos probado contra `AKIA…` de ejemplo.
- Sin tocar codigo de negocio (`app/`, `migrations/`, `backend/` intactos; solo
  `.github/`, `infra/ci/` y este reporte).

## Entorno
Rama `feature/ci-pipeline`. GitHub Actions.
