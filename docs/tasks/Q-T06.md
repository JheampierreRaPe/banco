# Q-T06 - Entorno Docker de demo y healthchecks

## Meta
| Campo | Valor |
|---|---|
| ID | `Q-T06` |
| Modulo | `devops` |
| Tipo | `devops` |
| Sprint | `1` |
| HU | `Transversal` |
| Dependencias | `Q-T05` |
| Estado | `Pendiente` |

## Objetivo
Dejar un `docker-compose` que levante Postgres, Redis, backend, worker y admin con healthchecks,
usando `.env.example` como plantilla.

## Contexto embebido
- Un solo comando debe levantar el entorno de demo.
- Sin secretos reales en el repo; usar `.env`.
- Healthcheck por modulo y global.

## Context pack
- L0. L1: `docs/modules/README.md#shared--config-infraestructura`.
- L2: `docs/15-calidad-seguridad-devops.md#5-entornos-y-contenedores`.

## Frontera
- Puede tocar: `docker-compose.yml`, `infra/docker/`, `.env.example`.
- Prohibido: codigo de negocio.

## Reglas
- Dependencias entre servicios con `depends_on` y healthchecks.
- Puertos y credenciales de ejemplo.

## CA
- Transversal; habilita las demos del sprint.

## Pruebas
- `docker compose up` levanta todo; `/health` responde.

## Entregables
- [ ] Compose + Dockerfiles + `.env.example`. [ ] Instructivo.

## Entorno
Rama `feature/docker-demo`. Docker local.
